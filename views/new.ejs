<%include partials/header%>

<div class="ui main text container segment">
        <div class="ui huge header">New Blog</div>
        <form class="ui form" action="/blogs" method="POST">
            <div class="field">
                <label>Title</label>
                <input required type="text" name="blog[title]" placeholder="Blog title">
            </div>
            <div class="field">
                    <label>Image URL</label>
                    <input required type="text" name="blog[image]" placeholder="Image link">
            </div>
            <div class="field">
                    <label>Title</label>
                    <textarea required name="blog[body]" placeholder="Body"></textarea>
            </div>
            <input class="ui inverted big primary button" type="submit">
        </form>
</div>

<%include partials/footer%>